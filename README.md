# Oh My Posh Salesforce Custom Theme for Powershell

This is a custom theme configuration for oh my posh. It builds on the [atomic](https://ohmyposh.dev/docs/themes#atomic) theme and adds the default target org to the terminl's prompt.

To use this theme, first set up `oh-my-posh`, then use the profile configuration.
